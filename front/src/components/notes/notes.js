import React from 'react';
import PropTypes from 'prop-types';
import { NotesWrapper } from './notes.styled';

const Notes = () => (
 <NotesWrapper data-testid="Notes">
    Notes Component
 </NotesWrapper>
);

Notes.propTypes = {};

Notes.defaultProps = {};

export default Notes;
